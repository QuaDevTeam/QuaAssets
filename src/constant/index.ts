// default size limit is 10MB
export const DEFAULT_SIZE_LIMIT = 10 * 1024 * 1024;

// default count limit is 100
export const DEFAULT_COUNT_LIMIT = 100;

// default ttl is 10 min
export const DEFAULT_CACHE_TTL = 10 * 60 * 1000;
